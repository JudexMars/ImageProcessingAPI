apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: redis-volume-tag
  name: redis-volume-tag
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
