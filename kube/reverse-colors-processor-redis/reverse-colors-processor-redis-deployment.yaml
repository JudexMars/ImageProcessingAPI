apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yaml
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: reverse-colors-processor-redis
  name: reverse-colors-processor-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: reverse-colors-processor-redis
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f compose.yaml
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: reverse-colors-processor-redis
    spec:
      containers:
        - args:
            - redis-server
            - --save
            - "20"
            - "1"
            - --logLevel
            - warning
            - --requirePass
            - 6k_j76,dDUl_
            - --port
            - "6381"
          image: redis:latest
          name: reverse-colors-processor-redis
          ports:
            - containerPort: 6381
              protocol: TCP
          volumeMounts:
            - mountPath: /data
              name: redis-volume-reverse-colors
      restartPolicy: Always
      volumes:
        - name: redis-volume-reverse-colors
          persistentVolumeClaim:
            claimName: redis-volume-reverse-colors
