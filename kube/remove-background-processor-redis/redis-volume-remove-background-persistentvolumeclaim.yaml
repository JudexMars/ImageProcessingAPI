apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: redis-volume-remove-background
  name: redis-volume-remove-background
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
