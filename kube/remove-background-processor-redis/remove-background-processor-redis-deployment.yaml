apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yaml
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: remove-background-processor-redis
  name: remove-background-processor-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: remove-background-processor-redis
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f compose.yaml
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: remove-background-processor-redis
    spec:
      containers:
        - args:
            - redis-server
            - --save
            - "20"
            - "1"
            - --logLevel
            - warning
            - --requirePass
            - 6k_j76,dDUl_
            - --port
            - "6382"
          image: redis:latest
          name: remove-background-processor-redis
          ports:
            - containerPort: 6382
              protocol: TCP
          volumeMounts:
            - mountPath: /data
              name: redis-volume-remove-background
      restartPolicy: Always
      volumes:
        - name: redis-volume-remove-background
          persistentVolumeClaim:
            claimName: redis-volume-remove-background
