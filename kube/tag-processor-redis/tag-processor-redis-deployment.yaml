apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yaml
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: tag-processor-redis
  name: tag-processor-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: tag-processor-redis
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f compose.yaml
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: tag-processor-redis
    spec:
      containers:
        - args:
            - redis-server
            - --save
            - "20"
            - "1"
            - --logLevel
            - warning
            - --requirePass
            - 6k_j76,dDUl_
            - --port
            - "6384"
          image: redis:latest
          name: tag-processor-redis
          ports:
            - containerPort: 6384
              protocol: TCP
          volumeMounts:
            - mountPath: /data
              name: redis-volume-tag
      restartPolicy: Always
      volumes:
        - name: redis-volume-tag
          persistentVolumeClaim:
            claimName: redis-volume-tag
