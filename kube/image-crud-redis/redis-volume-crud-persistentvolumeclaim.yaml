apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: redis-volume-crud
  name: redis-volume-crud
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
