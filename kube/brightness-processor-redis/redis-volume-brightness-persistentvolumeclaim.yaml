apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: redis-volume-brightness
  name: redis-volume-brightness
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
