apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: redis-volume-crop
  name: redis-volume-crop
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
